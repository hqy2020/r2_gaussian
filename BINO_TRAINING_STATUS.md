# Bino 训练状态报告

**生成时间**: 2025-11-20 16:17
**训练方法**: Binocular-Guided 3D Gaussian Splatting (NeurIPS 2024)
**训练视角**: 3 Views (稀疏视角重建)

---

## ✅ 训练任务概览

### 已启动的训练任务 (5/5)

| 器官 | PID | 状态 | 输出目录 | 日志文件 |
|------|-----|------|----------|----------|
| **Foot** | 2322023 | ✅ 运行中 | `output/2025_11_20_16_16_foot_3views_bino` | `output/2025_11_20_16_16_foot_3views_bino_train.log` |
| **Chest** | 2322173 | ✅ 运行中 | `output/2025_11_20_16_16_chest_3views_bino` | `output/2025_11_20_16_16_chest_3views_bino_train.log` |
| **Head** | 2322344 | ✅ 运行中 | `output/2025_11_20_16_16_head_3views_bino` | `output/2025_11_20_16_16_head_3views_bino_train.log` |
| **Abdomen** | 2322531 | ✅ 运行中 | `output/2025_11_20_16_16_abdomen_3views_bino` | `output/2025_11_20_16_16_abdomen_3views_bino_train.log` |
| **Pancreas** | 2322740 | ✅ 运行中 | `output/2025_11_20_16_16_pancreas_3views_bino` | `output/2025_11_20_16_16_pancreas_3views_bino_train.log` |

---

## 🔧 核心配置参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `iterations` | 30000 | 总训练迭代数 |
| `enable_opacity_decay` | `True` | 启用 Bino 不透明度衰减 |
| `opacity_decay_factor` | 0.995 | 衰减因子（论文推荐值） |
| `test_iterations` | `[1000, 5000, 10000, 15000, 20000, 25000, 30000]` | 评估检查点（**已修复**） |
| `save_iterations` | `[30000]` | 保存模型的迭代点 |
| `densify_from_iter` | 500 | 密化开始迭代 |
| `densify_until_iter` | 15000 | 密化结束迭代 |

---

## 🐛 问题诊断与修复

### 之前的问题

**症状**: 训练在 5000 步自动停止，未达到 30000 步

**根本原因**:
```yaml
# ❌ 错误的配置
test_iterations: [1000, 5000, 10000, 15000, 20000, 25000, 30000, 30000, 1]
                                                                           ↑
                                                                    导致异常的参数
```

**修复方案**:
```yaml
# ✅ 正确的配置
test_iterations: [1000, 5000, 10000, 15000, 20000, 25000, 30000]
```

---

## 📊 GPU 使用情况

当前 GPU 状态:
```
GPU 0: NVIDIA RTX A6000
  • 使用率: 100%
  • 显存占用: 14494 MB / 49140 MB (~29%)
  • 运行任务: 所有 5 个训练任务

GPU 1: NVIDIA RTX A6000
  • 使用率: 0%
  • 显存占用: 12 MB / 49140 MB
  • 运行任务: 空闲
```

**说明**: 所有训练任务都在 GPU 0 上运行，GPU 1 处于空闲状态。如果需要加速，可以考虑将部分任务分配到 GPU 1。

---

## 📈 预期训练时间

根据当前进度估算：

- **单个任务**: 约 30-60 分钟（30000 步）
- **所有任务**: 并行运行，约 30-60 分钟内全部完成

**评估检查点**:
- ✅ **1000 步**: 约 2-3 分钟后
- ✅ **5000 步**: 约 10-15 分钟后
- ⏳ **10000 步**: 约 20-30 分钟后
- ⏳ **30000 步**: 约 50-60 分钟后（最终结果）

---

## 🎯 SOTA 基准对比目标

| 器官 | R² Baseline PSNR | R² Baseline SSIM | Bino 目标 |
|------|------------------|------------------|-----------|
| Foot | **28.4873** | **0.9005** | 超越 baseline |
| Chest | 26.506 | 0.8413 | 超越 baseline |
| Head | 26.6915 | 0.9247 | 超越 baseline |
| Abdomen | 29.2896 | 0.9366 | 超越 baseline |
| Pancreas | 28.7669 | 0.9247 | 超越 baseline |

---

## 🔍 监控与管理

### 查看训练状态

```bash
# 运行监控脚本（推荐）
bash scripts/monitor_bino_training.sh

# 查看所有训练进程
ps aux | grep 'python train.py' | grep bino

# 查看 GPU 使用情况
nvidia-smi

# 查看所有 PID
cat output/*_bino.pid
```

### 查看训练日志

```bash
# 实时查看 foot 训练日志
tail -f output/2025_11_20_16_16_foot_3views_bino_train.log

# 实时查看 chest 训练日志
tail -f output/2025_11_20_16_16_chest_3views_bino_train.log

# 查看所有日志的最后 50 行
tail -50 output/*_bino_train.log
```

### 停止训练

```bash
# 停止单个任务（以 foot 为例）
kill 2322023

# 停止所有 Bino 训练任务
pkill -f 'python train.py.*bino'

# 或者逐个停止
kill 2322023  # foot
kill 2322173  # chest
kill 2322344  # head
kill 2322531  # abdomen
kill 2322740  # pancreas
```

---

## 📁 输出文件结构

每个训练任务的输出目录结构：

```
output/2025_11_20_16_16_{organ}_3views_bino/
├── cfg_args                      # 配置参数（文本格式）
├── cfg_args.yml                  # 配置参数（YAML 格式）
├── ckpt/                         # Checkpoint（如果启用）
├── eval/                         # 评估结果
│   ├── iter_001000/
│   │   ├── eval2d_render_test.yml    # 测试集 2D 评估
│   │   ├── eval2d_render_train.yml   # 训练集 2D 评估
│   │   └── eval3d.yml                # 3D 体积评估
│   ├── iter_005000/
│   ├── iter_010000/
│   ├── iter_015000/
│   ├── iter_020000/
│   ├── iter_025000/
│   └── iter_030000/              # 最终评估结果
└── events.out.tfevents.*         # TensorBoard 日志
```

---

## 📋 后续步骤

### 1. 等待训练完成 (~50-60 分钟)

定期运行监控脚本检查进度：
```bash
bash scripts/monitor_bino_training.sh
```

### 2. 查看最终结果

训练完成后（30000 步），查看评估结果：
```bash
# 查看所有器官的最终 2D 评估结果
for organ in foot chest head abdomen pancreas; do
    echo "=== $organ ==="
    cat output/2025_11_20_16_16_${organ}_3views_bino/eval/iter_030000/eval2d_render_test.yml | \
        grep -E "psnr_2d|ssim_2d" | grep -v "_projs"
done
```

### 3. 与 Baseline 对比

```bash
# 创建对比报告
python scripts/compare_with_baseline.py \
    --bino_results output/*_3views_bino/eval/iter_030000/ \
    --baseline_results baseline_results/
```

### 4. 可视化结果

```bash
# 启动 TensorBoard 查看训练曲线
tensorboard --logdir=output --port=6006

# 在浏览器打开: http://localhost:6006
```

---

## 📝 相关文档

- **使用指南**: `scripts/BINO_TRAINING_GUIDE.md`
- **监控脚本**: `scripts/monitor_bino_training.sh`
- **单任务训练**: `scripts/train_bino_foot3.sh`
- **批量训练**: `scripts/train_bino_batch.sh`
- **后台训练**: `scripts/train_bino_all_background.sh`

---

## ⚠️ 注意事项

1. **不要关闭终端**: 训练任务在后台运行，但如果你通过 SSH 连接，建议使用 `screen` 或 `tmux`
2. **定期检查**: 每 10-15 分钟运行一次监控脚本，确保训练正常
3. **磁盘空间**: 每个任务约占用 2-5 GB，确保有足够空间
4. **GPU 温度**: 如果长时间 100% 使用率，注意 GPU 温度

---

**最后更新**: 2025-11-20 16:17
**维护者**: Claude Code
**状态**: ✅ 所有任务正常运行
